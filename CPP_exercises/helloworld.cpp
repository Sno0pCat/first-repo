#include <iostream> /* the reason we do this is to inlcude all 
                        input and output functions and be able to use it
                    */

int main(){ // this is where the code starts

    //This is a comment
    /* This 
        is 
        a 
        multi
        step comment
        */

    std::cout << "I like chicken!" << std::endl; // always end the line with ;
    std::cout << "Its really good"<< '\n';
    return 0; // if the code runs with no error should return with 0 or else 1
}